#include <stdio.h>
#include <sting.h>
#include <stdlib.h>

int main()
{

  int array[10],i ;

  printf("The sample array is:");
  for (i=0; i<5; i++)
    { array[i]=i;

  printf(" %d:\n", array);
    }

  return 0;
}  
